---
max_turns: 6
allowed_tools: [Skill]
---

Write a JavaScript function `orderTotal(lines)` that takes an array of `{ price, quantity }` objects and returns the sum of `price * quantity` over them, skipping every line whose `quantity` is zero or negative. Write it as a plain `for` loop, and comment the body of the function for a teammate who will maintain it; a documentation block above the function is not what they asked for. Reply with the code alone, in one fenced block.
